[build]
  base = "/"
  command = "pnpm install && cd apps/web && pnpm run build"
  publish = "apps/web/.next"

[build.environment]
  NETLIFY_USE_YARN = "false"
  NODE_VERSION = "20"
  PNPM_FLAGS = "--shamefully-hoist"

[[plugins]]
  package = "@netlify/plugin-nextjs"
