[build]
  base = "/"
  command = "npm install -g pnpm && pnpm install && pnpm run build --filter=web"
  publish = "apps/web/.next"

[build.environment]
  NETLIFY_USE_YARN = "false"
  NODE_VERSION = "20"

[[plugins]]
  package = "@netlify/plugin-nextjs"
